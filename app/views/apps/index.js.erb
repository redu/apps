$('#apps').append('<%= escape_javascript render partial: "shared/app", collection: @apps, locals: { favorite_flag: false } %>');
$('#paginator').html("<%= escape_javascript(link_to_next_page(@apps, 'Mostrar mais...', {remote: true, params: {filter: @filter, search: @search}, class: 'button-default icon-arrow-down-pagination-gray_16_18-before'})) %>")
