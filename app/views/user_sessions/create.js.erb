<% if @valid_credentials %>
  window.location='<%= path_to_be_back %>';
<% else %>
  var $newForm = $("<%= escape_javascript render 'shared/form_sign_in' %>");
  $("#sign-in-form").replaceWith($newForm);
<% end %>
